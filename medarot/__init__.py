"""Fan-translation toolkit for *Medarot Card Robattle RB* (Nintendo Switch).

The package never contains game data: every module reads the user's own dump.
See ``docs/specs/`` for the behaviour each module is required to have.
"""

__version__ = "1.0.0"
